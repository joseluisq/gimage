sudo: false

language: php

php:
    - 7.0
    - 7.1

cache:
  directories:
    - $HOME/.composer/cache/files

install: travis_retry composer install --no-interaction --no-ansi --no-progress --no-suggest --optimize-autoloader --prefer-stable

script: make test

notifications:
    email: false

git:
    depth: 1
