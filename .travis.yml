sudo: false

language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm

before_script:
  - composer install --no-interaction --prefer-source --dev

script: phpunit --configuration config.xml --coverage-text

notifications:
  email: false
